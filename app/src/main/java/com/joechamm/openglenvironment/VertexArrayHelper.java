package com.joechamm.openglenvironment;

/*
public class VertexArrayHelper {

    private static final String TAG = "jcglenv:vtxarrhelp";

    // TODO: implement

    private final int mVAO;
    private final int mIndexBufferHandle;
    private final int[] mVertexBufferHandles;

    VertexArrayHelper(float[][] vertexData, short[] indices) {
        int[] handle = new int[1];

        int numVertexBuffers = vertexData.length;
        int indicesCount = indices.length;

        // create our vertex array object
        GLES32.glGenVertexArrays (1, handle, 0);
        mVAO = handle[0];

        // create the index buffer if we are passed an indices, otherwise we set it to - 1
        if ( indicesCount > 0 ) {


        }

        // create the buffers to associate
        mVertexBufferHandles = new int[numVertexBuffers];
        GLES32.glGenBuffers ( numVertexBuffers, mVertexBufferHandles, 0 );

        if ( hasIndexBuffer ) {
            GLES32.glGenBuffers ( 1, handle, 0 );
            mIndexBufferHandle = handle[ 0 ];
        } else {
            mIndexBufferHandle = - 1;
        }
    }

    public void
}
*/
